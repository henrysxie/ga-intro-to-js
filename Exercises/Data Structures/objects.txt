Create an object called `classData` with the following keys:

- "courseName", which should correspond to "Intro to Javascript"

- "studentCount", which should correspond to number of students, say 20

- "instructor", which should itself be a dictionary with the following keys
    - "name" ("Henry")
    - "gender" ("M")
    - "canProgram" (True)

- get the student count from the dictionary

- get the instructor name from the dictionary